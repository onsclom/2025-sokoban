{
  "name": "2025-sokoban",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun src/index.html --console",
    "build": "bun build src/index.html --outdir=dist --minify",
    "preview": "bunx serve dist",
    "generate-levels": "bun run src/generator/generate-top-levels.ts",
    "generate-stone-specs": "bun run src/generator/generate-stone-specs.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
